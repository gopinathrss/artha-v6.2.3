/**
 * IMAP connectivity is implemented in `src/lib/ingestion/orchestrator.ts`.
 * This module is reserved for V5.2 IntegrationProvider `comms.imap` test wiring.
 */
export const IMAP_IMPLEMENTATION_NOTE = 'Use ingestion orchestrator for live IMAP polling.'
