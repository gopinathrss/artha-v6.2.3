# ARTHA V4 — Audit checklist (repo scan)

**Date:** 2026-04-25  
**Scope:** Static/code review of `artha-v4` tree only (not runtime verification on a live DB unless noted).  
**Legend:** `[✓]` DONE · `[~]` PARTIAL · `[✗]` NOT IN REPO · `[?]` UNCERTAIN (needs manual test)

---

## DATABASE (11 new models from V4 spec Part 1)

| Item | Status | Notes |
|------|--------|--------|
| UserProfile model exists in schema.prisma | [✓] | `prisma/schema.prisma` |
| IncomeEvent model exists | [✓] | |
| ExpenseCommitment model exists | [✓] | |
| UpcomingEvent model exists | [✓] | |
| AllocationPlan model exists | [✓] | |
| SipExecution model exists | [✓] | |
| FXRate model exists | [✓] | |
| NavHistory model exists | [✓] | |
| HistoricalReturn model exists | [✓] | |
| SystemHealth model exists | [✓] | |
| AdvisorJournal model exists | [✓] | |
| All 11 migrations applied to artha_v4 database | [?] | **Cannot verify from repo alone.** Migration *files* present: `20260427095245_v4_cfo_foundation`, `20260428120000_v4_generated_report` (plus earlier init/v3). Apply with `prisma migrate deploy` on target DB. |
| npx prisma generate completed without errors | [?] | **Environment-dependent.** Windows/OneDrive has seen `EPERM` on engine rename; re-run locally to confirm. |

**Extra (not in “11” line list but in schema):** `GeneratedReport` model exists (reports flow).

---

## CURRENCY ENGINE (Part 2)

| Item | Status | Notes |
|------|--------|--------|
| src/lib/currency.ts exists | [✓] | |
| fetchAllRates() pulls from CNB, RBI, exchangerate-api fallback chain | [~] | **CNB** daily + **ECB** for INR/EUR + **exchangerate-api** (if `EXCHANGE_RATE_API_KEY` set), else hardcoded fallback. **No explicit RBI feed** in `fetchAllRates` (RBI data appears elsewhere, e.g. `indiaIntelligence` seed, not as FX chain source). |
| convertCurrency() works for all pairs CZK/EUR/USD/INR | [~] | Converts via CZK as hub; **not arbitrary cross-rates** without CZK (spec says “all pairs” — hub design; `convertCurrency` is CZK-centric). |
| formatCurrency() CZK with space separator | [✓] | Regex grouping + ` Kč` |
| formatCurrency() INR with lakh-style (₹12,34,567) | [~] | Uses `Intl` `en-IN` + `INR` — **verify** exact digit grouping vs fixed example. |
| Cron schedule for FX refresh wired to scheduler | [✓] | `src/lib/scheduler.ts` — `30 16 * * 1-5` → `fetchAllRates()` (Prague) |
| Top-right currency **dropdown** on every page | [~] | **Pill** that cycles CZK/EUR/INR + trust on **many** pages, not a literal `<select>` “dropdown” on 100% of pages without spot-check. |
| Selection persists to localStorage | [✓] | `artha_display_ccy` in `artha-ui.js` |
| Every monetary number on every page re-renders on currency change | [~] | **Partial:** `formatMoneyFromCzk` + `artha:fx-updated` used on key CFO views; **not** guaranteed for every legacy widget (e.g. portfolio table cells). |
| "Rates as of HH:MM CET" indicator visible | [✗] | **Not in UI** (no global stamp in `artha-ui` shell as specified). API exposes age elsewhere but not this exact label. |

---

## CFO BRAIN (Part 3)

| Item | Status | Notes |
|------|--------|--------|
| /finances page exists | [✓] | `src/dashboard/finances.html` |
| Section A: Recurring Income table + ADD + **modal** | [~] | **Table + add** via inline form; **not** a modal dialog. |
| Section B: Fixed commitments + **category icons** | [~] | **Table** + add; **no** icon set per category. |
| Section C: Upcoming events **calendar grid** | [✗] | **Table + forms**, not calendar grid. |
| /api/profile (GET, PATCH) | [~] | **GET** + **PUT** (upsert) exist; **no** `PATCH` route. |
| /api/income (GET, POST, PATCH, DELETE) | [~] | **GET, POST, PUT, DELETE**; **no** `PATCH` (PUT used for updates). |
| /api/expenses (GET, POST, PATCH, DELETE) | [~] | Same as income. |
| /api/events (GET, POST, PATCH, DELETE) | [~] | Same. |
| First-time onboarding wizard (5 steps) when no UserProfile | [✗] | **Not in repo** — **GET** `/api/profile` **auto-creates** a default `UserProfile` in non-demo; no 5-step wizard. |

---

## ALLOCATION PLANNER (Part 4)

| Item | Status | Notes |
|------|--------|--------|
| src/lib/allocationPlanner.ts exists | [✓] | |
| generateMonthlyPlan(monthYear) | [✓] | |
| Income for the month | [✓] | Incl. recurring + one-off logic |
| Fixed expenses (Q/Y amortization) | [✓] | MONTHLY / QUARTERLY / YEARLY |
| Reserved for events (90-day window) | [✓] | `reservedForEventsCzk` |
| Emergency fund top-up | [✓] | In plan as `emergencyTopupCzk` |
| Investable + deficit messaging | [✓] | `investableCzk` + deficit row in allocations |
| Picks instruments from InstrumentLibrary by score | [✓] | Equity/bonds from library + holdings |
| Avoids within 90d of tax-free (new buys) | [~] | **Skews / avoids funding new** top-eq if **existing** holding has tax &lt;90d; not identical to “all funds” rule everywhere. |
| Allocations with reasons | [✓] | `reason` on rows |
| Saves plan PROPOSED | [✓] | |
| /this-month page exists | [✓] | |
| **Three** columns: flow / recommended / system reasoning | [~] | **Grid** layout with cashflow, follow-through, allocations; **not** 3 equal “column” spec with dedicated reasoning column text for each row. |
| [Confirm] [Skip] [Edit] per row | [✗] | **Not in UI** (no per-row action buttons). |
| [Generate next month] locked until 5 days before salary | [✗] | **Not in repo** — only **Generate plan** for current/POST month body. |
| [I executed all] confirms entire plan | [✗] | **Not in repo** |
| Cron: salaryDay+1 06:00 generates if missing | [✗] | `scheduler.ts` has FX, morning job, monthly letter, backup; **no** plan cron line found. |
| “My salary arrived — generate now” | [~] | **Generate plan** / **POST /this-month/generate-now** exists; **copy/behavior** not identical to spec (no 409 on duplicate; creates **new** plan per run). |
| 409 if plan already exists (manual) | [✗] | **Not implemented** — new `AllocationPlan` created each generate. |
| Status “generated {date} — manual vs auto” | [~] | `planSource` on model; **UI** shows limited meta, not full spec string. |

---

## FOLLOW-THROUGH (Part 5)

| Item | Status | Notes |
|------|--------|--------|
| [Done] on row → modal amount+date | [✗] | **Not in repo** |
| [Skipped] with reason | [✗] | **Not in repo** |
| [Pending] default | [✗] | No per-row state machine in UI |
| Done → SipExecution EXECUTED | [✗] | API can **POST** sip-executions; **not** wired from plan row UI |
| Done → AdvisorJournal "FOLLOWED" | [✗] | **Not wired** from UI |
| EOM review cron 1st 09:00 | [✗] | **Not in scheduler** for this |
| Adherence % on dashboard | [✗] | **Not in repo** |
| Plan history per month + adherence | [✗] | **Not in repo** |

**[~]** `this-month` lists recent SIPs/journal as **read-only** (partial overlap with “visibility” only).

---

## INDIA MF (Part 6)

| Item | Status | Notes |
|------|--------|--------|
| Settings D2: India holdings CRUD | [~] | **India accounts** via `Accounts` in settings (`/api/accounts`); not labeled “D2” / holdings CRUD for MF as specified. |
| Quick-add presets (HDFC Top 100, etc.) | [✗] | **Not in repo** |
| /india: three sections NRE/NRO, MF, FD | [~] | **Page** exists; **layout** = analysis + table + NRI list — **not** 3 full sections w/ full MF/FD as specified. |
| India MF table + LTCG/STCG per holding | [✗] | **Not in repo** on `/india` |
| FD ladder + maturity calendar | [✗] | **Not in repo** |
| AMFI NAV cron 14:30 | [✗] | **Not in scheduler** for AMFI |
| AMFI parser NAVAll.txt | [✗] | **Not in repo** |

---

## AI INTELLIGENCE (Part 7)

| Item | Status | Notes |
|------|--------|--------|
| Anthropic primary, OpenAI fallback | [✓] | `src/lib/aiIntelligence.ts` |
| /api/intelligence/ask | [✓] | in `server.ts` |
| Full context (max 8000 tokens) | [~] | `buildFullContext` **caps ~2800 chars** + truncation — **not** 8000-token design. |
| Pre-computes by question type | [✓] | RETIREMENT, PROPERTY, FEES, MONTHLY_ACTION, etc. |
| System prompt with critical rules | [✓] | |
| Response stored; “context_snapshot” | [~] | Stored in **`AIMemory`** as **`portfolioSnapshot`**, not field name `context_snapshot`. |
| /intelligence page renders | [✓] | |
| 8 quick questions | [~] | **6** in `intelligence.html` `QUICK` array (not 8). |
| History last 20; feedback | [~] | API **take: 20**; UI shows **10** in history list; **feedback** endpoints exist. |

---

## REPORTS (Part 8 amended)

| Item | Status | Notes |
|------|--------|--------|
| POST /api/reports/generate + DB row | [✓] | `GeneratedReport` via `reportsService` |
| GET /reports/view/:id?token= | [✓] | `cfoRoutes.ts` + HTML |
| @media print + @page A4, page-break sections | [~] | **Minimal** print `window.print` path in HTML; **not** A4/section break spec in depth. |
| 10 sections + real data | [✗] | Report body is **JSON dump**-style, **not** 10 section report. |
| ECharts renderer svg | [✗] | **No ECharts** in report view. |
| Download PDF = window.print | [~] | “Print / PDF” link in template — **not** a dedicated [Download PDF] on `/reports` dashboard. |
| Reports list/history on /reports | [~] | **List** in table; **not** full history UX as spec. |

---

## RELIABILITY (Part 9)

| Item | Status | Notes |
|------|--------|--------|
| health.ts with **12** checks | [~] | **`runHealthChecks`** has **8** named checks, not 12. |
| /api/health all checks | [~] | Returns `checks` + `trustScore`. |
| Trust: (PASS×100 + WARN×50)/**12** | [~] | **Dynamic** `n = checks.length` (currently 8), not fixed `/12`. |
| Health table on Settings | [✗] | **Not in Settings page** (trust is topbar text only on pages). |
| Top-right **trust dot** on every page | [~] | **Text** “Trust X%” on pages with `trustLine`; **not** a “dot” component everywhere. |
| Per-page self-tests on load + degraded banner | [✗] | **Not in repo** beyond generic demo banner. |

---

## DESIGN UPGRADE (Part 10)

| Item | Status | Notes |
|------|--------|--------|
| Fraunces for display / hero | [~] | **Brand + headings** use Fraunces; **64px** net worth hero not verified as spec. |
| DM Sans body | [✓] | `artha-ui.css` |
| JetBrains Mono for data | [~] | **`.mono`**, tables, pill — not every digit. |
| Net worth 64px Fraunces 600 tabular | [✗] | `index` uses **`.h1` / `mono`**, not 64px Fraunces as specified. |
| ECharts replaces Chart.js | [✗] | **SVG path** in `index.html` — **not** ECharts, not Chart.js. |
| Period selector 1M/3M/6M/1Y/3Y/ALL | [~] | **6M / 1Y / ALL** on overview; **not** full set. |
| Phosphor Duotone (not Lucide) | [?] | **No** icon set audit (likely **no** Phosphor). |
| Dark + light pass review | [?] | **Theming exists**; formal **review** not automated. |
| Count-up animation on number update | [✗] | **Not in repo** |

---

## HISTORICAL DATA (Part 11)

| Item | Status | Notes |
|------|--------|--------|
| NavHistory for 30 library ETFs, 10y monthly | [✗] | **Models exist**; **seeding/Volume** not verified as spec. |
| CNB historical FX seeded | [✗] | **Not in repo** as 10y seed data |
| RBI repo history seeded | [~] | `indiaIntelligence` has seed/fetch patterns; **not** full “history” series as specified. |
| macro-events.yaml | [✗] | **Not found** |
| Monte Carlo function | [✗] | **Not in repo** (stress test file exists but not MC spec) |
| Stress (COVID, 2022, 2008) | [✗] | **Not in repo** as named scenarios |

---

## DEPLOYMENT (Part 12)

| Item | Status | Notes |
|------|--------|--------|
| deploy/setup.sh | [✗] | **No `deploy/`** directory in repo root scan |
| deploy/Caddyfile | [✗] | |
| deploy/ecosystem.config.js | [✗] | |
| deploy/backup.sh | [✗] | (Weekly backup is **in-app** `runWeeklyBackup` in triggers — not this file) |
| deploy/RUNBOOK.md | [✗] | |

---

## INTEGRATIONS (Part 13)

| Item | Status | Notes |
|------|--------|--------|
| Email save tests SMTP ✓/✗ | [?] | **Test email** path exists; **“save shows checkmark”** = UX not audited here. |
| Test email real send | [?] | `sendTestEmail` — **verify** in env. |
| Telegram save getMe + username | [?] | **If** implemented in settings — **not re-read** in this audit (likely partial). |
| Send test message | [?] | **Uncertain** without code path review. |
| Bot commands /start /summary/… | [✗] | **Not in repo** as a Telegram bot command router in this tree. |
| Daily digest 08:00 | [✗] | **Not in scheduler** as stated (morning 06:00 job exists, different). |

---

## TESTS (Part 14)

| Item | Status | Notes |
|------|--------|--------|
| Vitest | [✗] | **Jest** in `package.json` / `tests/unit` |
| Unit: currency 4ccy | [~] | `tests/unit/currency.format.test.ts` (exists) |
| Unit: xirr | [?] | `calculations.test.ts` — may cover; **not** a dedicated `xirr.ts` test file name. |
| Unit: allocationPlanner 10 scenarios | [✗] | **No** `allocationPlanner` unit test file found. |
| Unit: india tax | [✗] | **Not found** |
| API supertest all routes | [✗] | **Not found** |
| Playwright + E2E | [✗] | **Not in `package.json`** / no playwright config in scan |

**[~]** `tests/stress/multiYear.test.ts` exists (stress, not E2E).

---

## QUALITY GATES (Part 15)

| Gate | Status | Notes |
|------|--------|--------|
| 1 BUILD npm run build 0 errors | [?] | **Previously passed** in session; **re-run in CI** to lock. |
| 2 tsc --noEmit | [?] | `tsconfig.build` used; **separate** `--noEmit` not verified. |
| 3 Unit coverage > 70% | [✗] | **Jest** present; **coverage** not shown / unlikely at 70%. |
| 4–5 API/E2E | [✗] / [✗] | As above. |
| 6 DB migration clean | [?] | **Migrations on disk**; **DB** state unknown. |
| 7–19 | [?]/[✗] mix | **Mostly not met** or **unverified** without end-to-end verification (AI &lt;5s, print 10p, mobile 375, deploy, etc.) |

---

## Summary

- **Strong:** Core **Prisma** CFO + FX + **V4** dashboard + **CFO API surface** (PUT-based CRUD), **allocation engine**, **Claude+OpenAI** ask path, **reports** token view, **partial** V4 design/fonts/shell.  
- **Gaps vs checklist:** **No** 5-step onboarding, **no** follow-through modals/cron, **no** full India MF/AMFI, **no** 10-section/ECharts report, **no** deploy folder, **no** Vitest/Playwright/CI, **no** 12 health checks, **context** not 8k tokens, many **Part 5–6–11–12–15** items **absent** or **stub**.

**Next (user):** pick which **PARTIAL/NOT** rows to close first; this file is audit-only, no code changes in this step.
